// para deixar como observação, irei usar JavaScript, pois tenho mais afinidade em escrever códigos e manipulalos

let indice = 13;
let soma = 0;
let K = 0;

while (K < indice) {

    K = K + 1;

    soma = soma + K;

}

console.log(soma);

// resposta: após abrir o index.html no navegador e apertar a telca f12 e ir em console, verá o valor da soma = 91